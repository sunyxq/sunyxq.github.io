# 语言环境
language: node_js
# 需要sudo权限
sudo: required
# nodejs版本
node_js:
  - lts/*

# 指定缓存模块，可选。缓存可加快编译速度。
cache:
  directories:
    - node_modules

# 指定博客源码分支，因人而异。hexo博客源码托管在独立repo则不用设置此项
branches:
  only:
    - blog

before_install:
  - chmod +x deploy.sh

# 执行清缓存，生成网页操作
script:
  - ./deploy.sh
